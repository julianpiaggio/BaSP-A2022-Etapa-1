console.log('--EXCERCISE 1: VARIABLES Y OPERADORES');

/* Crear dos variables numéricas y utilizar el operador suma para guardar el valor de la suma de ambos números en una
3er variable.*/
console.log('-- EXCERCISE 1.A:');

var ageMark= 30;
var agePeter= 28;
var ageOverall = (ageMark + agePeter);
console.log(ageOverall);

/* Crear dos variables de tipo String y concatenarlas guardando el resultado en una 3er variable. */
console.log('-- EXCERCISE 1.B:');

var sentence = 'Sunday is';
var complete = 'going to be a sunny day.';
var sentenceComplete =(sentence + ' ' + complete);
console.log(sentenceComplete);

/* Crear dos variables de tipo String y sumar el largo de cada variable (cantidad de letras del string) guardando el
resultado de la suma en una 3er variable (utilizar length).*/
console.log('-- EXCERCISE 1.C:');

var phrase = 'today is a day ';
var phrase2 = 'that life is worth living';
var unitedPhrase =(phrase + phrase2);
console.log(unitedPhrase.length);